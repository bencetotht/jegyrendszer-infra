apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: web-imagepolicy
  namespace: default
spec:
  imageRepositoryRef:
    name: web-repo
  policy:
    semver:
      range: '>=1.0.0'
# ---
# apiVersion: traefik.io/v1alpha1
# kind: IngressRoute
# metadata:
#   name: grafana-public
#   namespace: monitoring
#   annotations:
#     kubernetes.io/ingress.class: traefik-external
#   labels:
#     env.skip: cloud
# spec:
#   entryPoints:
#     - web
#     - websecure
#   routes:
#   - kind: Rule
#     match: "Host(`grafana.bnbdevelopment.hu`) && PathPrefix(`/`)"
#     services:
#     - name: prometheus-release-grafana 
#       port: 80
---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: prometheus-release
  namespace: monitoring
  labels:
    env.skip: cloud
spec:
  interval: 5m
  chart:
    spec:
      chart: kube-prometheus-stack
      version: '77.5.0'
      sourceRef:
        kind: HelmRepository
        name: prometheus-repo
        namespace: flux-system
      interval: 1m
  values:
    grafana:
      enabled: false
      persistence:
        enabled: true
        storageClassName: "longhorn"
        accessModes:
          - ReadWriteMany
        annotations:
          helm.sh/resource-policy: "keep"
    prometheus:
      prometheusSpec:
        scrapeConfigSelector:
          matchLabels:
            release: prom
        podMonitorSelectorNilUsesHelmValues: false
        serviceMonitorSelectorNilUsesHelmValues: false
        serviceMonitorNamespaceSelector: {}
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    io.cilium/lb-ipam-ips: 192.168.88.109
  labels:
    release: prometheus-release
  name: prometheus-release-kube-pr-prometheus-lb
  namespace: monitoring
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: prometheus
    operator.prometheus.io/name: prometheus-release-kube-pr-prometheus
  ports:
  - name: http-web
    port: 9090
    protocol: TCP
    targetPort: 9090
  - appProtocol: http
    name: reloader-web
    port: 8080
    protocol: TCP
    targetPort: reloader-web